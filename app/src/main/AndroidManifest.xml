<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nuru">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main_"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_main__round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nuru"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".View.Activity.NewMyFarmActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.ShowImageActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.EditCommunityActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.CommunityContentsActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.AddCommunityActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.AddAdminActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.AddFarmActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.AddImageActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.MyFarmActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.MyPageActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.SearchAddressActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activity.SearchAddressActivity2"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_push" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/light_green" />

        <activity
            android:name=".View.Activity.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".View.Activity.MapsActivity2"
            android:exported="false" />
        <activity
            android:name=".View.Activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Activity.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".View.Activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>